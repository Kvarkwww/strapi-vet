// config/plugins.js

module.exports = ({ env }) => ({
    /*email: {
      config: {
        provider: 'nodemailer',
        providerOptions: {
          host: env('SMTP_HOST', 'mail.kvark.si'),
          port: env('SMTP_PORT', 465),
          secure: true,
          auth: {
            user: env(''),
            pass: env(''),
          },
        },
        settings: {
          defaultFrom: 'mail@kvark.si',
          defaultReplyTo: 'info@kvark.si',
        },
      },
    }, */
  });